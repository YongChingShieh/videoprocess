# 核心依赖
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Flask>=2.3.0
requests>=2.31.0
Pillow>=10.0.0
PyNvVideoCodec
pymilvus==2.6.3

# 协议缓冲区相关（pymilvus依赖）
protobuf==3.20.0
grpcio-tools>=1.56.0
grpcio>=1.56.0

# 开发工具（可选）
# 如果不需要代理功能，可以移除 requests[socks]
requests[socks]>=2.31.0